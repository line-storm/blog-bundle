{% extends 'LineStormBlogBundle:Admin:index.html.twig' %}

{% block module %}
    <h1>{{ post.title }}</h1>
    {% for tag in  post.tags %}<label class="label label-primary">{{ tag.name }}</label>{% endfor %}
    <hr />


{% endblock %}
